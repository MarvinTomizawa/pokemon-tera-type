import types from "./common/types";
import status from "../common/status";

export const BaseEnemy = {
    name: "",
    moves: [
        {
            name: '',
            type: types,
            status: status,
        }
    ],
    types: [types],
};